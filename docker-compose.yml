version: "3.9"

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - db_data:/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/desk-booking.db

volumes:
  db_data:
    driver: local
